<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="../css/addMember.css">
    <title>New Member</title>
</head>
<body>
    <nav class="navigation">
        <h1><%= nama %></h1>
        <a href="/homeAdmin">Home</a>
        <a href="/ManageMember">Manage Members</a>
        <a href="/">Log out</a>
    </nav>
    <h1 class="add-member-title">
        Tambah Member Baru
    </h1>
    <div class="form">
        <div class="form-inner">
            <form class="" method="post" action="/addMember">
                <div class="form-input">
                    <label for="email" class="">Nama :</label>
                    <input type="text" name="nama" placeholder="Nama" required="">
                </div>
                <div class="form-input">
                    <label for="email" class="">Username :</label>
                    <input type="username" name="user" placeholder="Username" required="">
                </div>
                <div class="form-input">
                    <label for="email" class="">Password :</label>
                    <input type="password" name="pass" placeholder="Username" required="">
                </div>
                <div class="form-input">
                    <label for="email" class="">Alamat :</label>
                    <input type="text" name="alamat" placeholder="Alamat" required="">
                </div>
                <div class="alamat">
                    <div class="dropdown">
                        <label>Kota :</label>
                        <select name="filterkota" id="filter-kota">
                            <% for(let data of dataKota) { %>
                            <option value="<%= data.kota %>"><%= data.kota %></option>
                            <% } %>
                        </select>
                    </div>
                    <div class="dropdown">
                        <label>Kecamatan :</label>
                        <select name="filterkec" id="filter-kec">
                            <% for(let data of dataKecamatan) { %>
                            <option value="<%= data.kecamatan %>"><%= data.kecamatan %></option>
                            <% } %>
                        </select>
                    </div>
                    <div class="dropdown">
                        <label>Kelurahan :</label>
                        <select name="filterkel" id="filter-kel">
                            <% for(let data of dataKelurahan) { %>
                            <option value="<%= data.kelurahan %>"><%= data.kelurahan %></option>
                            <% } %>
                        </select>
                    </div>
                </div>
                <button type="submit" class="member">Save</button>
            </form>
        </div>
    </div>
</body>
</html>